#include <stdio.h>
#include <string.h>

#define MAX 80

int main()
{
    char c, palavra[100], palavra_inv;
    int i, j = 0, leng;

    scanf("%s", palavra);

    leng = strlen(palavra);

    for (i = leng - 1; i = 0; i--)
    {
        palavra_inv[j] = palavra[i];
    }

    return 0;
}